import styled from "styled-components";
import COLORS from "./Colors";

const MutedArtickle = styled.article`
  background-color: ${COLORS.mutedArtickle};
  padding: 3rem 0 3rem;
`;

export default MutedArtickle;
